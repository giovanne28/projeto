# funções (def)

def soma (a, b, c, d, e, f, g):
      total = a + b + c + d + e + f + g
        return total
    soma (28, 14, 2005, 6, 8, 2021, 2024)

def media (a, b, c):
      total = a + b + c / 3
        return total
    n1 = float(input('nota:'))
    n2 = float(input('nota:'))
    n3 = float(input('nota:'))
    media (n1, n2, n3)
